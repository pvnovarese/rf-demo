services:
  web:
    build:
      context: ./app
      secrets: 
        - cg_username
        - cg_password
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/todos
      SECRET_KEY: ${SECRET_KEY}
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./app:/app

  db:
    #image: postgres:15
    image: reg.mini.dev/postgres:15.19@sha256:b2d7f18493d5554eb66c942b75bf8519d1221bbfe8dbb7fe4b7db589114f0db5
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: todos
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

secrets:
  cg_username:
    environment: CHAINGUARD_PYTHON_IDENTITY_ID
  cg_password:
    environment: CHAINGUARD_PYTHON_TOKEN

volumes:
  postgres_data:
